// function greet(name){
//     console.log(`hello ${name}`);
// }
// greet("SLX")

// -------------------------------------------------------------
// Questions:-
// ------------

// 1) Define a function called multiply that takes two parameters: x and y. The function should return the product of x and y. Then, call the function with arguments 4 and 5 and log the result.

    // function multiply(x , y){
    //     return x * y
    // }
    // console.log(multiply(10, 10));

// 2) Create a function fullName that takes two parameters: firstName and lastName. Inside the function, return the full name in the format: "firstName lastName". Call the function with the arguments "John" and "Doe", and log the full name.
    
    //  function fullName(firstName, lastName){
    //     return `${firstName} ${lastName}`
    //  }
    //  console.log(fullName("Sufail", "SLX"));
     
// 3) Write a function called calculateArea that takes two parameters: length and width. The function should return the area of a rectangle. Additionally, add a check to ensure the arguments are positive numbers. If the arguments are not valid, return an error message.
 
    //   function calculateArea(length, width){
    //       if(length <= 0 || width <=0){
    //         return "Error"
    //       }
    //         return length * width
    //   }
    //   console.log(calculateArea(-1, 5));
      

