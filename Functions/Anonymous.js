      // const functionName = function(param){
     //     return param
    // }
   // console.log(functionName(param));

// -------------------------------------------------------------

// Questions:-
// ------------

// 1) Create an anonymous function that takes two numbers as arguments and returns their product. Assign this function to a variable and test it with two numbers.

//  const sum = function(a, b){
    //          return a * b;
    //  }
    //  console.log(sum(10, 10));
    
    // 2) Using an anonymous function, write a program that takes an array of strings and returns a new array with each string converted to uppercase.    
    
    // const strings = ["Apple", "slx"];
    // const uppercaseStrings = strings.map(function(str) {
        //   return str.toUpperCase();
        // });
        // console.log(uppercaseStrings);
        
        // 3) Write an anonymous function that filters out all numbers from an array that are divisible by 3 and returns the remaining numbers. Use the filter() method./

        //  const off = [1,2,3,4,5,6]
    //  const divisible = off.filter(function(num){
    //     return num % 3 !== 0
    //  })
    
    //  console.log(divisible);
    
    // -------------------------------------------------------------