// condition ? expression1 : expression2 

// let age = 18;

// let msg = age <= 18 ? "You Can Vote" : "You Can't Vote"

// console.log(msg);

// -------------------------------------------------------------
// Questions:-
// ------------

// 1) Write a ternary operator to check if a number is even or odd.

    // let no = 10;

    // let check = no % 2 == 0 ? "it's even no" : "it's odd no"
    
    // console.log(check)

// 2) Write a ternary operator to check if a number is positive, negative, or zero

    //  let no = 100

    //  let check = no > 0 ? "Postive" : no < 0 ? "Negative" : "Zero "
     
    //  console.log(check);
     
// 3) Write a ternary operator to assign a grade based on a student's score. If the score is 90 or above, assign 'A', if it's between 80 and 89, assign 'B', and if it's below 80, assign 'C'.
     
    // let grade = 96

    // let res = grade >= 90 ? "A" : grade >= 80 ? "B" : "C"

    // console.log(res);
    
    
    